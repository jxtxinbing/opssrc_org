/* NOTE: This file is just to avoid the linkage issues for CMake file,
 *       Remove it after adding the ops-tunnel code and change the CMake
 *       file accordingly.
 */

#include <stdio.h>

int main(void)
{
    printf("OPS-TUNNEL!\n");
    return 0;
}
